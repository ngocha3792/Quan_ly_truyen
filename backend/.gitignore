# Root .gitignore ignores migration SQL. This scoped override keeps real Prisma migrations versioned.
!prisma/migrations/**/*.sql

# Local and production secrets must never be committed.
.env.production
docker/.env.docker
backups/
ops/production/secrets/
.env.staging
ops/production/.deployment-state/
