diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 9fc4317..14c8b4b 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -549,14 +549,11 @@ jobs:
         working-directory: backend
         run: npm ci --no-audit --no-fund
 
-      - name: Migrate and seed E2E database
+      - name: Migrate E2E database
         working-directory: backend
         run: |
           npm run ci:wait-db
           npm run db:migrate:deploy
-          npm run db:seed
-          npm run db:seed:e2e
-          npm run db:seed:e2e:admin
 
       - name: Build and start backend
         working-directory: backend
@@ -581,7 +578,7 @@ jobs:
 
       - name: Run Playwright E2E
         working-directory: frontend
-        run: npx playwright test
+        run: npm run e2e:ci
 
       - name: Upload E2E diagnostics
         if: failure()
diff --git a/backend/package.json b/backend/package.json
index d4928f6..5899dbf 100644
--- a/backend/package.json
+++ b/backend/package.json
@@ -32,7 +32,7 @@
     "test:watch": "jest --watch",
     "test:cov": "jest --coverage",
     "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
-    "test:e2e": "jest --config ./test/jest-e2e.json",
+    "test:e2e": "npm run test:e2e:prepare && jest --config ./test/jest-e2e.json --runInBand",
     "test:auth:unit": "jest --config ./test/jest-auth-unit.json --runInBand",
     "test:auth:prepare": "tsx scripts/testing/prepare-auth-test-environment.ts",
     "test:auth:integration": "npm run test:auth:prepare && jest --config ./test/jest-auth-integration.json --runInBand",
@@ -102,7 +102,9 @@
     "docker:prod:gate:postdeploy": "docker compose --env-file .env.production -f compose.production.yml --profile tools run --rm gate-postdeploy",
     "observability:up": "docker compose --env-file .env.production -f ops/observability/docker-compose.observability.yml up -d",
     "observability:down": "docker compose --env-file .env.production -f ops/observability/docker-compose.observability.yml down",
-    "observability:config": "docker compose --env-file .env.production -f ops/observability/docker-compose.observability.yml config"
+    "observability:config": "docker compose --env-file .env.production -f ops/observability/docker-compose.observability.yml config",
+    "db:seed:e2e:all": "npm run db:seed && npm run db:seed:e2e && npm run db:seed:e2e:admin && npm run db:seed:e2e:stories",
+    "test:e2e:prepare": "tsx scripts/testing/prepare-postgres-test-environment.ts"
   },
   "dependencies": {
     "@nestjs/bullmq": "^11.0.4",
diff --git a/backend/scripts/testing/prepare-auth-test-environment.ts b/backend/scripts/testing/prepare-auth-test-environment.ts
index f8696df..a2788fa 100644
--- a/backend/scripts/testing/prepare-auth-test-environment.ts
+++ b/backend/scripts/testing/prepare-auth-test-environment.ts
@@ -81,8 +81,9 @@ void runScript({
     try {
       await redis.connect();
       await redis.ping();
+      await redis.flushdb();
 
-      logger.info('Redis test database is reachable', {
+      logger.info('Redis test database is reachable and clean', {
         database: redisDatabase,
       });
 
diff --git a/frontend/e2e/admin/author-applications.spec.ts b/frontend/e2e/admin/author-applications.spec.ts
index 2e8e44d..9ab151b 100644
--- a/frontend/e2e/admin/author-applications.spec.ts
+++ b/frontend/e2e/admin/author-applications.spec.ts
@@ -114,6 +114,8 @@ test('reviewer từ chối hồ sơ và lưu rejection reason', async ({ page })
     hasText: 'E2E Reject Pen',
   });
 
+  await expect(row).toBeVisible();
+
   await row
     .getByRole(
       'link',
diff --git a/frontend/e2e/fixtures/authenticated-test.ts b/frontend/e2e/fixtures/authenticated-test.ts
index b63bbc6..cde48b0 100644
--- a/frontend/e2e/fixtures/authenticated-test.ts
+++ b/frontend/e2e/fixtures/authenticated-test.ts
@@ -1,6 +1,7 @@
 import { expect, test as base } from '@playwright/test';
 
 import { E2E_USER_EMAIL, E2E_USER_PASSWORD } from './e2e-user';
+import { runBackendScript } from '../support/backend-script';
 
 const SESSION_HINT_KEY = 'truyenhub.auth.has-refresh-session';
 
@@ -12,6 +13,11 @@ export const test = base.extend({
 
     testInfo,
   ) => {
+    // Mỗi test/retry local bắt đầu từ cùng một user/session/role state.
+    if (process.env['E2E_EXTERNAL'] !== 'true') {
+      runBackendScript('db:seed:e2e');
+    }
+
     const response = await page.request.post(
       '/api/v1/auth/login',
 
diff --git a/frontend/e2e/fixtures/managed-admin-test.ts b/frontend/e2e/fixtures/managed-admin-test.ts
index 24930bc..acbf783 100644
--- a/frontend/e2e/fixtures/managed-admin-test.ts
+++ b/frontend/e2e/fixtures/managed-admin-test.ts
@@ -1,6 +1,7 @@
 import { expect, test as base } from '@playwright/test';
 
 import { E2E_MANAGER_EMAIL, E2E_MANAGER_PASSWORD } from './e2e-manager';
+import { runBackendScript } from '../support/backend-script';
 
 const SESSION_HINT_KEY = 'truyenhub.auth.has-refresh-session';
 
@@ -12,6 +13,11 @@ export const test = base.extend({
 
     testInfo,
   ) => {
+    // Admin scenarios mutate users/applications; reset before every local test/retry.
+    if (process.env['E2E_EXTERNAL'] !== 'true') {
+      runBackendScript('db:seed:e2e:admin');
+    }
+
     const response = await page.request.post(
       '/api/v1/auth/login',
 
diff --git a/frontend/e2e/global-setup.ts b/frontend/e2e/global-setup.ts
index bfe3946..fabdf5b 100644
--- a/frontend/e2e/global-setup.ts
+++ b/frontend/e2e/global-setup.ts
@@ -1,73 +1,15 @@
-import { existsSync } from 'node:fs';
-
-import { spawnSync } from 'node:child_process';
-
-import path from 'node:path';
+import { runBackendScript } from './support/backend-script';
 
 export default function globalSetup(): void {
   /**
-   * Khi test server bên ngoài,
-   * không được tự động đụng DB local.
+   * External E2E có thể trỏ vào staging/production-like environment.
+   * Tuyệt đối không tự động seed database ở chế độ này.
    */
   if (process.env['E2E_EXTERNAL'] === 'true') {
     return;
   }
 
-  const backendDirectory = resolveBackendDirectory();
-
-  console.log('[Playwright] Chuẩn bị dữ liệu E2E...');
-
-  runBackendScript(backendDirectory, 'db:seed');
-
-  runBackendScript(backendDirectory, 'db:seed:e2e');
-}
-
-function resolveBackendDirectory(): string {
-  const candidates = [
-    /**
-     * Khi cwd = frontend
-     */
-    path.resolve(process.cwd(), '../backend'),
-
-    /**
-     * Khi cwd = repository root
-     */
-    path.resolve(process.cwd(), 'backend'),
-  ];
-
-  const backendDirectory = candidates.find((candidate) =>
-    existsSync(path.join(candidate, 'package.json')),
-  );
-
-  if (!backendDirectory) {
-    throw new Error(
-      ['Không tìm thấy thư mục backend.', 'Playwright cần backend để seed E2E user.'].join(' '),
-    );
-  }
+  console.log('[Playwright] Chuẩn bị dữ liệu E2E deterministic...');
 
-  return backendDirectory;
-}
-
-function runBackendScript(cwd: string, script: string): void {
-  const npmCommand = process.platform === 'win32' ? 'npm.cmd' : 'npm';
-
-  const result = spawnSync(npmCommand, ['run', script], {
-    cwd,
-
-    env: process.env,
-
-    stdio: 'inherit',
-
-    shell: false,
-  });
-
-  if (result.error) {
-    throw result.error;
-  }
-
-  if (result.status !== 0) {
-    throw new Error(
-      `Backend script "${script}" thất bại với exit code ${result.status ?? 'unknown'}.`,
-    );
-  }
+  runBackendScript('db:seed:e2e:all');
 }
diff --git a/frontend/e2e/support/backend-script.ts b/frontend/e2e/support/backend-script.ts
new file mode 100644
index 0000000..2abf669
--- /dev/null
+++ b/frontend/e2e/support/backend-script.ts
@@ -0,0 +1,54 @@
+import { spawnSync } from 'node:child_process';
+import { existsSync } from 'node:fs';
+import path from 'node:path';
+
+let cachedBackendDirectory: string | undefined;
+
+export function runBackendScript(script: string): void {
+  const cwd = resolveBackendDirectory();
+  const npmCommand = process.platform === 'win32' ? 'npm.cmd' : 'npm';
+
+  const result = spawnSync(npmCommand, ['run', script], {
+    cwd,
+    env: process.env,
+    stdio: 'inherit',
+    shell: false,
+  });
+
+  if (result.error) {
+    throw result.error;
+  }
+
+  if (result.status !== 0) {
+    throw new Error(
+      `Backend script "${script}" thất bại với exit code ${result.status ?? 'unknown'}.`,
+    );
+  }
+}
+
+function resolveBackendDirectory(): string {
+  if (cachedBackendDirectory) {
+    return cachedBackendDirectory;
+  }
+
+  const candidates = [
+    path.resolve(process.cwd(), '../backend'),
+    path.resolve(process.cwd(), 'backend'),
+  ];
+
+  const backendDirectory = candidates.find((candidate) =>
+    existsSync(path.join(candidate, 'package.json')),
+  );
+
+  if (!backendDirectory) {
+    throw new Error(
+      ['Không tìm thấy thư mục backend.', 'Playwright cần backend để chuẩn bị dữ liệu E2E.'].join(
+        ' ',
+      ),
+    );
+  }
+
+  cachedBackendDirectory = backendDirectory;
+
+  return backendDirectory;
+}
diff --git a/frontend/package.json b/frontend/package.json
index 0f09c46..b30e74e 100644
--- a/frontend/package.json
+++ b/frontend/package.json
@@ -20,10 +20,10 @@
     "e2e:headed": "playwright test --headed",
     "e2e:ui": "playwright test --ui",
     "e2e:report": "playwright show-report",
-    "e2e:prepare": "npm --prefix ../backend run db:seed && npm --prefix ../backend run db:seed:e2e && npm --prefix ../backend run db:seed:e2e:admin && npm --prefix ../backend run db:seed:e2e:stories",
-    "pree2e": "npm run e2e:prepare",
+    "e2e:prepare": "npm --prefix ../backend run db:seed:e2e:all",
     "quality:check": "npm run format:check && npm run architecture:check && npm run lint:check && npm run typecheck && npm run typecheck:e2e",
-    "ci": "npm run env:check && npm run quality:check && npm run test:ci && npm run build:ci"
+    "ci": "npm run env:check && npm run quality:check && npm run test:ci && npm run build:ci",
+    "e2e:ci": "playwright test"
   },
   "private": true,
   "packageManager": "npm@11.12.1",
diff --git a/frontend/playwright.config.ts b/frontend/playwright.config.ts
index 51b52a2..816fbbc 100644
--- a/frontend/playwright.config.ts
+++ b/frontend/playwright.config.ts
@@ -11,6 +11,8 @@ const baseURL = isExternalServer
 export default defineConfig({
   testDir: './e2e',
 
+  globalSetup: './e2e/global-setup.ts',
+
   /**
    * Auth E2E hiện dùng chung một account:
    *
